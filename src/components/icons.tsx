import { Icon, IconElement } from '@ui-kitten/components';

import React from 'react';

export const HomeIcon = (props: any): IconElement => <Icon {...props} name='home-outline' />;

export const ExploreIcon = (props: any): IconElement => <Icon {...props} name='layers-outline' />;

export const EmailIcon = (props: any): IconElement => <Icon {...props} name='email-outline' />;

export const SearchIcon = (props: any): IconElement => <Icon {...props} name='search-outline' />;
export const BackIcon = (props: any): IconElement => <Icon {...props} name='arrow-back' />;
